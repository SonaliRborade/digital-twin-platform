spring:
  application:
    name: observer-service

  data:
    mongodb:
      uri: mongodb://localhost:27017/digital_twin_db

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

  endpoint:
    health:
      show-details: always
